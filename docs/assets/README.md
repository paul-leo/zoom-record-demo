# docs/assets

Media for the docs.

- `demo-walkthrough.gif` — screen/replay capture of `examples/electron-demo`
  running the full flow (Record now → chunks → saved file/segments → detect →
  resume), captured via harness-fe session replay. See
  [../recording-lifecycle.md](../recording-lifecycle.md#walkthrough-recording)
  for how to (re)capture it.
